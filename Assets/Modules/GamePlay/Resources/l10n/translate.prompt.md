ID가 _S{int} 접미사를 가지고 있고, 이 접미사를 제거했을 때의 ID가 이미 존재한다면(이미 존재하는 것을 origin data라고 한다.), 접미사가 달린 열을 제거하여라.
_S{int} ID는 두번째 열의 문자열 안에서 변수({}로 마킹된다)가 존재할 때, 변수를 TTS에 녹음시키지 않게 하기 위해 string을 자른 것이다. 이 부분은 언어마다 다르므로, origin data로부터 새롭게 생성하여라.
